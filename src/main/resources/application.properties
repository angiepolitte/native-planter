spring.application.name=native-plant

# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/native-plant
spring.datasource.username=${USERNAME}
spring.datasource.password=${PASSWORD}

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

perenual.api.key=sk-vnyO67ba08a1348818792
















## Database connection settings
#spring.datasource.url=jdbc:mysql://${APP_DB_HOST}:${APP_DB_PORT}/${APP_DB_NAME}
#spring.datasource.username=${APP_DB_USER}
#spring.datasource.password=${APP_DB_PASS}
#
## Specify the DBMS
#spring.jpa.database = MYSQL
#
## Show or not log for each sql query
#spring.jpa.show-sql = false
#
## Hibernate ddl auto (create, create-drop, update)
#spring.jpa.hibernate.ddl-auto = update
#
## Use spring.jpa.properties.* for Hibernate native properties (the prefix is
## stripped before adding them to the entity manager)
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
